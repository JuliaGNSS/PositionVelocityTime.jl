var documenterSearchIndex = {"docs":
[{"category":"section","location":"api/#API-Reference","page":"API Reference","text":"","title":"API Reference"},{"category":"section","location":"api/#Types","page":"API Reference","text":"","title":"Types"},{"category":"section","location":"api/#PVT-Computation","page":"API Reference","text":"","title":"PVT Computation"},{"category":"section","location":"api/#Position-and-Velocity","page":"API Reference","text":"","title":"Position and Velocity"},{"category":"section","location":"api/#Atmospheric-Corrections","page":"API Reference","text":"These corrections are applied automatically by calc_pvt; they are documented here for reference and for diagnostic use.","title":"Atmospheric Corrections"},{"category":"section","location":"api/#Dilution-of-Precision","page":"API Reference","text":"The DOP values are read from the dop field of a PVTSolution, e.g. pvt.dop.GDOP.","title":"Dilution of Precision"},{"category":"section","location":"api/#Utilities","page":"API Reference","text":"","title":"Utilities"},{"category":"section","location":"api/#The-Measurement-Model-Surface","page":"API Reference","text":"The pieces calc_pvt is assembled from, documented so a consumer that runs its own estimator over the same measurement model — a navigation filter closing tracking loops, for instance — reuses this package's model as a stated contract. The names are deliberately not exported (they are solver internals, not part of every user's vocabulary); bind them explicitly with using PositionVelocityTime: calc_corrected_time, …. Times returned or taken here are seconds-of-week counts on each satellite's own system scale unless a function says otherwise.\n\nThe unexported internals the surface above links to, documented for reference:","title":"The Measurement-Model Surface"},{"category":"section","location":"api/#Multi-GNSS-Classification","page":"API Reference","text":"When calc_pvt combines constellations and bands it classifies each satellite by three keys, all provided by GNSSSignals (3.3+) and read from a satellite's ranging signal: GNSSSignals.get_time_system (a GNSSSignals.TimeSystem, i.e. GPST()/GST()/BDT()) drives receiver-clock grouping (one bias per time system); GNSSSignals.get_band_id (e.g. :L1, :L5) drives inter-frequency-bias grouping (one bias per band); and GNSSSignals.get_signal_id (e.g. :GPSL1CA) is the per-signal identity used in the sats key of PVTSolution. This package forwards each of the latter two to a SatelliteState.\n\nThe receiver inter-frequency biases and their reference bands (reported per InterFrequencyBias) are laid out from the constellation × band coverage graph:","title":"Multi-GNSS Classification"},{"category":"type","location":"api/#PositionVelocityTime.SatelliteState","page":"API Reference","text":"SatelliteState{CP<:Real,D<:GNSSDecoderState,S<:AbstractGNSSSignal}\n\nCombines the GNSS decoder state with code and carrier phase measurements for a single satellite.\n\nFields\n\ndecoder::GNSSDecoderState: GNSS decoder state containing decoded navigation data\nsystem::AbstractGNSSSignal: GNSS system (e.g., GPSL1CA(), GalileoE1B())\ncode_phase::CP: Code phase measurement in chips\ncarrier_doppler: Carrier Doppler frequency in Hz\ncarrier_phase::CP: Carrier phase measurement in radians, matching Tracking.get_carrier_phase (default: 0.0)\n\nConstructors\n\nSatelliteState(; decoder, system, code_phase, carrier_doppler, carrier_phase=0.0)\nSatelliteState(decoder, system, sat_state)\n\nThe second constructor extracts code phase, carrier Doppler, and carrier phase from a Tracking satellite state (Tracking.TrackedSat). It is provided by a package extension that is loaded automatically once Tracking is available, so Tracking is only a weak dependency of this package.\n\n\n\n\n\n","title":"PositionVelocityTime.SatelliteState"},{"category":"type","location":"api/#PositionVelocityTime.PVTSolution","page":"API Reference","text":"PVTSolution\n\nComplete Position, Velocity, and Time solution from GNSS measurements.\n\nFields\n\nposition::ECEF: User position in ECEF coordinates (meters)\nvelocity::ECEF: User velocity in ECEF coordinates (m/s)\ncourse_over_ground::typeof(1.0°): Horizontal direction of travel (degrees), the azimuth of the velocity vector in the local East-North-Up frame at position — measured clockwise from true North and wrapped to [0, 360)°, following the GNSS course-over-ground (COG) convention. Derived from the velocity alone, so this is the direction of motion, not vehicle heading (which a single-antenna receiver cannot observe). The vertical (Up) velocity component is ignored. 0° when the horizontal velocity is zero (stationary or purely vertical), where course is undefined.\ntime_correction::typeof(1.0m): Estimated receiver clock bias of the reference GNSS (meters). For a multi-GNSS solution this is the bias of reference_system; other systems' biases are time_correction + inter_system_biases[system].\ntime::Union{TAIEpoch{Float64}, Nothing}: Estimated time as a TAI epoch\nrelative_clock_drift::Float64: Relative receiver clock drift (dimensionless)\ndop::Union{DOP, Nothing}: Dilution of precision values\nsats::Dictionary{Tuple{Symbol, Int}, SatInfo}: Maps (signal, PRN) to satellite info — position, transmit time, and the post-fit pseudorange and range-rate residuals (see SatInfo). The signal tag (e.g. :GPSL1CA, :GalileoE1B; see get_signal_id) keeps the same PRN apart both across constellations (GPS PRN 5 vs Galileo E05) and across signals of one constellation (a satellite tracked on GPS L1 C/A and L5 yields two entries sharing a PRN). Receiver-clock grouping is by time system, not signal — see reference_system.\nreference_system::Union{GNSSSignals.TimeSystem, Nothing}: GNSS time system (e.g. GNSSSignals.GPST(), GST()) that time and time_correction are referenced to.\ninter_system_biases::Dict{GNSSSignals.TimeSystem, typeof(1.0m)}: For each GNSS time system other than reference_system, the offset of that system's time scale relative to the reference system's (meters) — the inter-system bias. This is a system / space-segment effect, not a receiver one (this receiver has no inter-system hardware bias): it is the GNSS system-time offset, so the Galileo entry equals −c · Δt_systems, Δt_systems = GST − GPST (the GGTO), and the BeiDou entry likewise carries the BDT steering residual (the BGTO's subject) — never the defined 14 s count offset, which is a convention already removed from the measurements (see calc_time_scale_offsets). It is estimated directly from the geometry whenever observable (no broadcast error); the broadcast GGTO/BGTO is used to derive it only as a fallback (the offset-aided collapse), as that broadcast value may be erroneous. Reference-independent (the difference of two entries is the offset between those two systems); empty for a single-system solution.\ninter_frequency_biases::Dict{Symbol, InterFrequencyBias}: For each frequency band other than the reference band, the receiver inter-frequency bias relative to it — the differential hardware delay of that band's RF chain, estimated as an extra unknown when satellites are processed on more than one band. The key is the band (e.g. :L1, :L5; see get_band_id) and the value is shared across all constellations on that band. Each InterFrequencyBias carries both the bias (in metres) and the reference band it is measured against — the anchor whose delay is folded into the clock biases — so each reported bias's anchor is explicit rather than implicit. The reference is chosen per coverage component (see band_ifb_layout), so a disconnected constellation can in principle yield several references. Empty for a single-band solution. A solution then needs n ≥ 3 + M + B satellites for M time systems and B extra bands.\n\n\n\n\n\n","title":"PositionVelocityTime.PVTSolution"},{"category":"type","location":"api/#PositionVelocityTime.SatInfo","page":"API Reference","text":"SatInfo\n\nPer-satellite information attached to a PVTSolution (one entry per satellite used in the fix).\n\nFields\n\nposition::ECEF: Satellite ECEF position at transmit time (metres).\ntime::Float64: Satellite transmit time (system time of week, seconds).\nresidual::typeof(1.0m): Post-fit least-squares pseudorange residual (metres) — the (atmosphere-corrected) measured minus the modeled pseudorange. A per-satellite fit-quality / outlier indicator.\nrate_residual::typeof(1.0m/s): Post-fit least-squares range-rate residual (metres per second) — the measured minus the modeled range rate of the carrier-Doppler velocity and clock-drift solve. The rate-domain counterpart of residual: it flags a satellite whose Doppler disagrees with the velocity fix (cycle slips, dynamics) independently of its pseudorange.\n\nBoth are measured minus modeled (\"observed minus computed\"), the orientation GNSS software reports observation residuals in — RTKLIB's rescode / resdop, and GNSS-SDR and PocketSDR through it. Note that rate_residual follows resdop in the geometric range-rate sense, positive while the satellite recedes; a receiver forming the same residual from its tracking loops' λ · carrier_doppler works in the opposite sign (see calc_user_velocity_and_clock_drift, whose yⱼ sets that sign).\n\n\n\n\n\n","title":"PositionVelocityTime.SatInfo"},{"category":"type","location":"api/#PositionVelocityTime.InterFrequencyBias","page":"API Reference","text":"InterFrequencyBias\n\nA receiver inter-frequency bias attached to a PVTSolution: the differential hardware delay of one frequency band's RF chain, together with the band it is measured against.\n\nFields\n\nvalue::typeof(1.0m): The bias (metres), relative to reference — how much longer this band's receiver chain delay is than the reference band's.\nreference::Symbol: The reference band (e.g. :L1; see get_band_id) whose delay is folded into the clock biases and against which value is measured. Chosen per coverage component (see band_ifb_layout), so different biases in a disconnected solution can in principle carry different references.\n\n\n\n\n\n","title":"PositionVelocityTime.InterFrequencyBias"},{"category":"function","location":"api/#PositionVelocityTime.calc_pvt","page":"API Reference","text":"calc_pvt(states::AbstractVector{<:SatelliteState},\n         prev_pvt::PVTSolution = PVTSolution();\n         approximate_year::Integer = year(now(UTC)),\n         enable_ionospheric_correction::Bool = true,\n         enable_tropospheric_correction::Bool = true) -> PVTSolution\n\nCalculate Position, Velocity, and Time (PVT) from GNSS satellite measurements.\n\nSatellites from different GNSS and frequency bands may be combined. Because each constellation references its broadcasts to its own system time, one receiver clock bias is estimated per GNSS time system; and because the receiver's RF chain delays each band differently, one receiver inter-frequency bias is estimated per frequency band beyond a reference band (shared across constellations on that band; see get_band_id). The state vector is therefore [x, y, z, tc₁, …, tc_M, ifb₁, …, ifb_B] for M distinct time systems and B extra bands. Position and time are found by least squares; velocity and clock drift are solved from carrier Doppler.\n\nA solution requires n ≥ 3 + M + B healthy satellite measurements (each system needs at least one satellite, and a system contributing a single satellite spends it entirely on that system's clock bias), of which 3 + M must come from distinct satellites: a satellite tracked on several bands supplies one line of sight, and its extra measurements constrain the inter-frequency biases rather than the geometry. When either condition fails, constellations whose messages carry a broadcast offset to another tracked system — Galileo's GGTO (Galileo–GPS Time Offset), BeiDou's BGTO (BDT–GNSS Time Offset, toward GPS or Galileo), or the GGTO GPS itself broadcasts on CNAV/CNAV-2 — have their clock bias collapsed onto that hub system using the broadcast offset, which makes a 4-satellite mixed fix possible (see decide_bias_layout for the hub order). Estimating an independent bias is preferred whenever the geometry allows it, since it avoids the broadcast offset's own error.\n\nUnless disabled via enable_ionospheric_correction, the ionospheric delay is corrected automatically using only the coefficients decoded from the navigation messages. A single model is chosen for the whole solve and applied to every satellite, preferring the more accurate global TEC models: NTCM-G if Galileo Effective Ionisation Level coefficients have been decoded, else BDGIM if a BDS-3 B-CNAV (B1C/B2a/B2b) coefficient set has, else the GPS Klobuchar model, else BeiDou's own Klobuchar variant from its legacy B1I/B3I message, else no correction. See select_ionospheric_correction and ionospheric_delay.\n\nUnless disabled via enable_tropospheric_correction, the tropospheric delay is corrected with a blind model (no broadcast coefficients needed): Saastamoinen zenith delays mapped to the line of sight by the Niell mapping functions. See tropospheric_delay.\n\nArguments\n\nstates: Vector of SatelliteState for observed satellites. Each (signal, PRN) pair must appear at most once — a receiver produces one measurement per signal per satellite, and a duplicate would enter the least-squares solve twice.\nprev_pvt: Previous PVT solution used as initial guess (default: origin). A previous solution whose own DOP is implausible (GDOP < 0, or PDOP above MAX_TRUSTED_WARM_START_PDOP = 50) is not used as a seed and the epoch is solved from cold, so one spurious fix cannot re-seed itself epoch after epoch.\n\nKeyword Arguments\n\napproximate_year: Calendar year of the observation, used to resolve the GPS L1 C/A 1024-week rollover ambiguity (legacy LNAV broadcasts only a 10-bit week number, so the receiver needs external information to determine which 1024-week cycle the recording is in). Anything within ±9 years of the actual observation date works. Defaults to the current UTC year, which is correct for live signals; for processing archived recordings, pass the rough year of the recording.\nenable_ionospheric_correction: when true (default), apply the automatic ionospheric correction described above. Set to false to skip it entirely and solve from the raw pseudoranges (e.g. for diagnostics or when an external correction is applied elsewhere).\nenable_tropospheric_correction: when true (default), apply the Saastamoinen tropospheric correction. Set to false to skip it.\n\nReturns\n\nA PVTSolution containing position, velocity, time, DOP values, and satellite information. Returns prev_pvt if the epoch cannot be solved: too few healthy satellites to solve the constellation (including the GGTO fallback and the distinct-satellite condition — a satellite tracked on several bands supplies one line of sight, so measurements alone are not enough), a geometry whose solved design matrix is rank deficient (reported as a negative GDOP). None of these throw, so a receiver can pass whatever it currently tracks each epoch and carry prev_pvt forward. Distrust of prev_pvt (see above) affects only the seed: an unsolvable epoch still returns prev_pvt exactly as passed.\n\n\n\n\n\n","title":"PositionVelocityTime.calc_pvt"},{"category":"function","location":"api/#PositionVelocityTime.get_LLA","page":"API Reference","text":"get_LLA(pvt::PVTSolution) -> LLA\n\nConvert the ECEF position in the PVT solution to geodetic coordinates (latitude, longitude, altitude) using the WGS84 ellipsoid.\n\n\n\n\n\n","title":"PositionVelocityTime.get_LLA"},{"category":"function","location":"api/#PositionVelocityTime.calc_satellite_position","page":"API Reference","text":"calc_satellite_position(decoder::GNSSDecoder.GNSSDecoderState, t)\ncalc_satellite_position(state::SatelliteState)\n\nCalculate the satellite ECEF position from orbital parameters at time t.\n\nThe first method takes a decoder state and explicit time. The second method computes the corrected transmission time from the SatelliteState automatically.\n\nArguments\n\ndecoder: GNSS decoder state containing ephemeris data\nt: Transmission time in system time (seconds)\nstate: A SatelliteState combining decoder, system, and phase measurements\n\nReturns\n\nAn SVector{3, Float64} with the satellite position in ECEF coordinates (meters).\n\n\n\n\n\n","title":"PositionVelocityTime.calc_satellite_position"},{"category":"function","location":"api/#PositionVelocityTime.calc_satellite_position_and_velocity","page":"API Reference","text":"calc_satellite_position_and_velocity(decoder::GNSSDecoder.GNSSDecoderState, t)\ncalc_satellite_position_and_velocity(state::SatelliteState)\n\nCalculate the satellite ECEF position and velocity from orbital parameters at time t.\n\nUses Keplerian orbital mechanics with perturbation corrections (harmonic corrections for argument of latitude, radius, and inclination) to propagate the satellite ephemeris.\n\nArguments\n\ndecoder: GNSS decoder state containing ephemeris data\nt: Transmission time in system time (seconds)\nstate: A SatelliteState combining decoder, system, and phase measurements\n\nReturns\n\nA named tuple (position, velocity) where each is an SVector{3, Float64} in ECEF coordinates (meters and m/s respectively).\n\n\n\n\n\n","title":"PositionVelocityTime.calc_satellite_position_and_velocity"},{"category":"function","location":"api/#PositionVelocityTime.get_sat_enu","page":"API Reference","text":"get_sat_enu(user_pos_ecef::ECEF, sat_pos_ecef::ECEF) -> Spherical\nget_sat_enu(enu_from_ecef::ENUfromECEF, sat_pos_ecef::ECEF) -> Spherical\n\nConvert satellite position to East-North-Up (ENU) spherical coordinates (azimuth and elevation) relative to the user position.\n\nArguments\n\nuser_pos_ecef::ECEF: User position in ECEF coordinates\nenu_from_ecef::ENUfromECEF: A precomputed ENUfromECEF(user_pos_ecef, wgs84) transform. Pass this form to reuse one transform across all satellites that share a user position (one geodetic conversion per epoch instead of per satellite).\nsat_pos_ecef::ECEF: Satellite position in ECEF coordinates\n\nReturns\n\nSpherical coordinates containing azimuth and elevation of the satellite as seen from the user position.\n\n\n\n\n\n","title":"PositionVelocityTime.get_sat_enu"},{"category":"function","location":"api/#PositionVelocityTime.select_ionospheric_correction","page":"API Reference","text":"select_ionospheric_correction(states)\n    -> Union{KlobucharParams,BeiDouKlobucharParams,NTCMGParams,BDGIMParams,Nothing}\n\nScan all (healthy) satellite decoders and pick the single ionospheric correction to apply to the whole solve, in a fixed order of preference:\n\nNTCM-G, if Galileo Effective Ionisation Level coefficients were decoded.\nBDGIM, if a BDS-3 B-CNAV (B1C/B2a/B2b) coefficient set was decoded.\nKlobuchar from GPS, if the GPS α/β were decoded.\nBeiDou's own Klobuchar variant from its legacy D1/D2 message (B1I/B3I).\nnothing — no coefficients at all, so no correction.\n\nThe two global TEC models come first because they are the better ones: BDGIM is a spherical-harmonic TEC expansion, whereas Klobuchar is the eight-coefficient half-cosine approximation IS-GPS-200 only undertakes to remove ~50 % of the delay with. The rungs among equals are tie-breaks rather than claims — NTCM-G stays ahead of BDGIM so a Galileo-bearing epoch behaves exactly as it did before, and GPS stays ahead of BeiDou among the Klobuchar sets as it already did — but every rung is fixed rather than data-dependent, so the chosen model does not flip with the order the receiver happens to hand satellites over in. The coefficients are global to a constellation, so the first decoder that carries each set is used.\n\n\n\n\n\n","title":"PositionVelocityTime.select_ionospheric_correction"},{"category":"function","location":"api/#PositionVelocityTime.ionospheric_delay","page":"API Reference","text":"ionospheric_delay(correction, system, elevation, azimuth, lla, time_of_week) -> Float64\n\nSlant ionospheric group delay in metres for one satellite (system is the satellite's GNSS, used for its carrier frequency), using the constellation-wide correction returned by select_ionospheric_correction:\n\n::Nothing → 0.0 (no coefficients were decoded).\nKlobucharParams → Klobuchar model (IS-GPS-200, Fig. 20-4).\nBeiDouKlobucharParams → BeiDou's Klobuchar variant (BDS-SIS-ICD-B1I-3.0 §5.2.4.7).\nNTCMGParams → NTCM-G model.\nBDGIMParams → BDGIM (BDS-SIS-ICD-B1C-1.0 §7.8.2).\n\nThe line of sight is given by the satellite elevation/azimuth (radians) and the user geodetic position lla (a Geodesy.LLA); time_of_week is the measurement's system time of week in seconds. Which constellation's scale that is does not matter: GPST and GST coincide to within the sub-microsecond GGTO, and BDT trails them by 14 s, which moves Klobuchar's diurnal phase by 0.016 % of a day, NTCM-G's universal time by the same, and BDGIM's solar longitude by 0.06 milliradians — orders of magnitude below any of the three models' own error. The geometry is taken precomputed — and shared across satellites and with tropospheric_delay — so a whole-epoch correction does the user geodetic conversion only once. Derive the geometry from ECEF with LLAfromECEF(wgs84)(user) and _elevation_azimuth(ENUfromECEF(user, wgs84), sat).\n\n\n\n\n\n","title":"PositionVelocityTime.ionospheric_delay"},{"category":"function","location":"api/#PositionVelocityTime.tropospheric_delay","page":"API Reference","text":"tropospheric_delay(elevation, lla, doy; humidity = 0.7) -> Float64\n\nSlant tropospheric group delay in metres for a satellite at elevation (radians) seen from the user geodetic position lla (a Geodesy.LLA): the Saastamoinen zenith delays (see saastamoinen_zenith_delays) mapped to the line of sight by the Niell mapping functions (see niell_mapping_functions), whose seasonal term is driven by the day of year doy. The delay is non-dispersive (frequency independent). humidity is the relative humidity (0…1) used for the wet component. The delay is finite and monotone in elevation all the way down to the horizon (≈ 90 m for a grazing ray at sea level); elevations below the horizon reuse the horizon value. Returns 0.0 when the user height is outside the model's valid range (−100 m … 10 km).\n\nThe geometry is taken precomputed so a whole-epoch correction shares the user geodetic conversion and the elevation with ionospheric_delay.\n\n\n\n\n\n","title":"PositionVelocityTime.tropospheric_delay"},{"category":"function","location":"api/#PositionVelocityTime.saastamoinen_zenith_delays","page":"API Reference","text":"saastamoinen_zenith_delays(latitude, height, humidity) -> (hydrostatic, wet)\n\nSaastamoinen zenith hydrostatic and wet tropospheric delays in metres, from a standard atmosphere (15 °C at sea level) evaluated at the geodetic latitude (radians) and height (metres, clamped below at 0). humidity is the relative humidity (0…1) feeding the water-vapour partial pressure of the wet term. The formulas are those of RTKLIB's tropmodel at zenith.\n\n\n\n\n\n","title":"PositionVelocityTime.saastamoinen_zenith_delays"},{"category":"function","location":"api/#PositionVelocityTime.niell_mapping_functions","page":"API Reference","text":"niell_mapping_functions(latitude, height, elevation, doy) -> (hydrostatic, wet)\n\nThe Niell (1996) hydrostatic and wet mapping functions: dimensionless factors that map the zenith tropospheric delays to a satellite at elevation (radians). latitude is in radians, height is the site's ellipsoidal height in metres (it enters the hydrostatic height correction; RTKLIB likewise substitutes the ellipsoidal height for the orthometric one), and doy is the day of year driving the hydrostatic seasonal term (phase-shifted half a year in the southern hemisphere). Elevations below the horizon are evaluated at the horizon, where both factors are finite (≈ 37 hydrostatic, ≈ 57 wet); both decrease strictly to exactly 1 at zenith.\n\n\n\n\n\n","title":"PositionVelocityTime.niell_mapping_functions"},{"category":"type","location":"api/#PositionVelocityTime.KlobucharParams","page":"API Reference","text":"KlobucharParams(α_0, α_1, α_2, α_3, β_0, β_1, β_2, β_3)\n\nThe eight Klobuchar ionospheric coefficients decoded from a GPS L1 navigation message, in IS-GPS-200 SI units (seconds and seconds·semicircle⁻ⁿ). The field names mirror GNSSDecoder.GPSL1CAData (α_0…α_3, β_0…β_3).\n\n\n\n\n\n","title":"PositionVelocityTime.KlobucharParams"},{"category":"function","location":"api/#PositionVelocityTime.klobuchar_group_delay","page":"API Reference","text":"klobuchar_group_delay(φ_u, λ_u, E, A, gps_time, α, β)\n\nKlobuchar single-frequency ionospheric group delay for GPS L1 (IS-GPS-200N, Fig. 20-4), returned in seconds. All angles are in semicircles: φ_u/λ_u are the user geodetic latitude/longitude, E/A the satellite elevation/azimuth. gps_time is GPS system time in seconds. α/β are the 4-element Klobuchar coefficient tuples (SI units).\n\n\n\n\n\n","title":"PositionVelocityTime.klobuchar_group_delay"},{"category":"type","location":"api/#PositionVelocityTime.BeiDouKlobucharParams","page":"API Reference","text":"BeiDouKlobucharParams(α_0, α_1, α_2, α_3, β_0, β_1, β_2, β_3)\n\nThe eight coefficients of BeiDou's own Klobuchar variant, decoded from the legacy D1/D2 message (B1I/B3I), in BDS-SIS-ICD-B1I-3.0 §5.2.4.7 SI units (seconds and seconds·π⁻ⁿ — per semicircle, exactly like the GPS set). A type of its own rather than a KlobucharParams because the ICD prescribes a different algorithm, not merely different numbers — see beidou_klobuchar_group_delay — and defines the resulting delay at the B1I carrier (1561.098 MHz) rather than L1.\n\n\n\n\n\n","title":"PositionVelocityTime.BeiDouKlobucharParams"},{"category":"function","location":"api/#PositionVelocityTime.beidou_klobuchar_group_delay","page":"API Reference","text":"beidou_klobuchar_group_delay(φ_u, λ_u, E, A, t_E, α, β)\n\nBeiDou's Klobuchar-variant ionospheric group delay along the B1I propagation path (BDS-SIS-ICD-B1I-3.0 §5.2.4.7), returned in seconds. All angles are in radians, the ICD's working unit: φ_u/λ_u are the user geodetic latitude/longitude, E/A the satellite elevation/azimuth. t_E is the BDT second of week. α/β are the 4-element coefficient tuples (SI units; their s·π⁻ⁿ scale factors make the polynomial argument φ_M/π — semicircles, exactly like the GPS set).\n\nThe same eight-coefficient half-cosine idea as klobuchar_group_delay, but not the same algorithm — the ICD replaces IS-GPS-200's fitted approximations with exact spherical geometry and drops the geomagnetic frame:\n\nThe Earth-central angle to the ionospheric pierce point is the exact π/2 − E − arcsin(R/(R+h)·cos E) at R = 6378 km, h = 375 km, and the obliquity the matching exact 1/√(1 − (R/(R+h)·cos E)²), instead of the GPS model's 0.0137/(E+0.11) − 0.022 and 1 + 16(0.53−E)³ fits.\nThe latitude feeding the A₂/A₄ polynomials is the pierce point's geographic latitude from the spherical triangle — no +0.064·cos(λ−1.617) geomagnetic conversion, no ±0.416-semicircle clamp — and its longitude uses arcsin rather than a flat-Earth division.\nThe period A₄ is clamped from above at 172800 s as well as from below at 72000 s, and the cosine is evaluated exactly rather than by the two-term Taylor expansion inside |x| < 1.57.\n\nThe 5·10⁻⁹ s night floor and the |t − 50400| < A₄/4 day-time window are those of the GPS model.\n\n\n\n\n\n","title":"PositionVelocityTime.beidou_klobuchar_group_delay"},{"category":"type","location":"api/#PositionVelocityTime.NTCMGParams","page":"API Reference","text":"NTCMGParams(a_i0, a_i1, a_i2, week_number::Integer)\n\nThe broadcast Galileo Effective Ionisation Level coefficients a_i0/a_i1/a_i2 (sfu, sfu/deg, sfu/deg²) decoded from an E1B navigation message, together with the GST week number needed to derive the day of year and universal time for NTCM-G.\n\n\n\n\n\n","title":"PositionVelocityTime.NTCMGParams"},{"category":"type","location":"api/#PositionVelocityTime.BDGIMParams","page":"API Reference","text":"BDGIMParams(α_1, …, α_9, week_number::Integer)\n\nThe nine broadcast BDGIM coefficients decoded from a BDS-3 B-CNAV message (B1C/B2a/B2b), in the ICD's units of TECu, together with the BDT week number.\n\nThe names are the ICD's own α_1 … α_9 (BDS-SIS-ICD-B1C-1.0 Table 7-10); GNSSDecoder spells them α_bdgim_1 … α_bdgim_9 on its BeiDou containers to keep them apart from the legacy D1/D2 Klobuchar α_0 … α_3, and already applies each one's scale factor and sign — including α_5's negative scale factor of −2⁻³ — so they arrive here as plain signed TECu.\n\nweek_number is needed for the same reason NTCMGParams carries one: the model's time argument is a date (a Modified Julian Date), which a time of week alone cannot supply. Unlike the Klobuchar set there is no reference frequency, because BDGIM yields TEC rather than a delay at a particular carrier.\n\n\n\n\n\n","title":"PositionVelocityTime.BDGIMParams"},{"category":"function","location":"api/#PositionVelocityTime.klobuchar_params","page":"API Reference","text":"klobuchar_params(decoder) -> Union{KlobucharParams,BeiDouKlobucharParams,Nothing}\n\nKlobuchar α/β decoded from a GPS navigation message (LNAV GPSL1CAData, CNAV GPSCNAVData on L5/L2C, or CNAV-2 GPSL1C_DData) or from the BeiDou legacy D1/D2 message (BeiDouDNAVData on B1I/B3I), or nothing if they have not been broadcast yet or the decoder carries no Klobuchar set. The same single-frequency Klobuchar model is broadcast on all GPS civil signals; BeiDou broadcasts its own set, returned as a BeiDouKlobucharParams because its ICD prescribes its own variant of the algorithm, not just its own numbers.\n\n\n\n\n\n","title":"PositionVelocityTime.klobuchar_params"},{"category":"function","location":"api/#PositionVelocityTime.ntcm_g_params","page":"API Reference","text":"ntcm_g_params(decoder) -> Union{NTCMGParams,Nothing}\n\nNTCM-G Effective Ionisation Level coefficients decoded from a Galileo navigation message (I/NAV on E1-B or E5b, or F/NAV on E5a — all broadcast a_i0…a_i2), or nothing if they (or the week number) have not been decoded yet or the decoder carries no such set. Galileo E6-B is in the latter group: C/NAV broadcasts no ionospheric coefficients, so it falls to the generic method rather than being dispatched here on a field it does not have.\n\n\n\n\n\n","title":"PositionVelocityTime.ntcm_g_params"},{"category":"function","location":"api/#PositionVelocityTime.bdgim_params","page":"API Reference","text":"bdgim_params(decoder) -> Union{BDGIMParams,Nothing}\n\nThe nine BDGIM coefficients decoded from a BDS-3 B-CNAV message — B-CNAV1 on B1C, B-CNAV2 on B2a, B-CNAV3 on B2b, all three broadcasting the same set — or nothing if they, or the week number the model needs to date its epoch, have not been decoded yet, or the decoder carries no such set. The legacy D1/D2 message (BeiDouDNAVData, on B1I/B3I) is in the latter group: it broadcasts Klobuchar instead, so it falls to this generic method rather than being dispatched on fields it does not have.\n\n\n\n\n\n","title":"PositionVelocityTime.bdgim_params"},{"category":"function","location":"api/#PositionVelocityTime._elevation_azimuth","page":"API Reference","text":"_elevation_azimuth(enu_from_ecef::ENUfromECEF, sat_position) -> (elevation, azimuth)\n\nElevation and azimuth (radians) of sat_position (ECEF) in the local East-North-Up frame defined by enu_from_ecef = ENUfromECEF(user_position, wgs84). Azimuth is measured clockwise from North. The transform is taken precomputed so it can be built once per user position and reused across satellites.\n\n\n\n\n\n","title":"PositionVelocityTime._elevation_azimuth"},{"category":"type","location":"api/#PositionVelocityTime.DOP","page":"API Reference","text":"DOP\n\nDilution of Precision (DOP) values describing the geometric quality of the satellite constellation used for a PVT solution.\n\nFields\n\nGDOP::Float64: Geometric DOP (overall quality)\nPDOP::Float64: Position DOP (3D position quality)\nVDOP::Float64: Vertical DOP\nHDOP::Float64: Horizontal DOP\nTDOP::Float64: Time DOP\n\n\n\n\n\n","title":"PositionVelocityTime.DOP"},{"category":"function","location":"api/#PositionVelocityTime.get_sat_info","page":"API Reference","text":"get_sat_info(pvt_solution::PVTSolution, signal::Symbol, prn::Integer) -> Union{SatInfo,Nothing}\n\nReturn the SatInfo (position, transmit time and the post-fit pseudorange/range-rate residuals) of the satellite with the given prn on GNSS signal (e.g. :GPSL1CA, :GalileoE1B; see get_signal_id), or nothing if that satellite was not used in the fix. The signal tag is required because the same PRN can belong to different constellations or be tracked on several signals; see the sats field of PVTSolution.\n\n\n\n\n\n","title":"PositionVelocityTime.get_sat_info"},{"category":"constant","location":"api/#PositionVelocityTime.SPEED_OF_LIGHT","page":"API Reference","text":"SPEED_OF_LIGHT\n\nThe speed of light (m/s), shared by every range/time conversion in this package and its consumers.\n\n\n\n\n\n","title":"PositionVelocityTime.SPEED_OF_LIGHT"},{"category":"function","location":"api/#PositionVelocityTime.calc_corrected_time","page":"API Reference","text":"calc_corrected_time(state::SatelliteState) -> Float64\n\nThe satellite's transmit time as a seconds-of-week count on its own system's scale: the decoder's time of week plus the bit, code-phase and carrier-phase fractions (calc_uncorrected_time), corrected by the SV clock polynomial, the relativistic term and the ranging signal's group delay (correct_clock). This is the t every ephemeris and clock evaluation takes; only pseudorange differencing converts it to another scale (see time_scale_offset_to_gpst), never the value itself.\n\n\n\n\n\n","title":"PositionVelocityTime.calc_corrected_time"},{"category":"function","location":"api/#PositionVelocityTime.calc_satellite_clock_drift","page":"API Reference","text":"calc_satellite_clock_drift(decoder::GNSSDecoderState, t) -> Float64\n\nRate of the satellite clock correction (s/s): the time derivative of the clock polynomial correct_clock applies, so it must be evaluated about the same clock reference epoch t_0c. Using t instead would offset the a_f2 term by a spurious 2·a_f2·t_0c — zero whenever a_f2 = 0 (the usual GPS broadcast) but up to ~4e-9 s/s (≈1.3 m/s of range rate) at the edge of the broadcast range late in the week, and per-satellite, so it would leak into the estimated velocity rather than the common clock drift.\n\nThe rate of the relativistic periodic term Δt_rel = F·e·√A·sin(E) that correct_clock includes is not modelled here; it is at most ~1 mm/s.\n\n\n\n\n\n","title":"PositionVelocityTime.calc_satellite_clock_drift"},{"category":"function","location":"api/#PositionVelocityTime.get_sat_position","page":"API Reference","text":"get_sat_position(sat_pv) -> SVector{3,Float64}\n\nSatellite ECEF position (m) of one calc_satellite_position_and_velocity result.\n\n\n\n\n\n","title":"PositionVelocityTime.get_sat_position"},{"category":"function","location":"api/#PositionVelocityTime.get_sat_velocity","page":"API Reference","text":"get_sat_velocity(sat_pv) -> SVector{3,Float64}\n\nSatellite ECEF velocity (m/s) of one calc_satellite_position_and_velocity result.\n\n\n\n\n\n","title":"PositionVelocityTime.get_sat_velocity"},{"category":"function","location":"api/#PositionVelocityTime.fold_week_crossover","page":"API Reference","text":"fold_week_crossover(t) -> Float64\n\nFold a difference of two seconds-of-week counts modulo the week, into (-302400, 302400]: near a week rollover the two operands sit on opposite sides of the wrap and their raw difference is off by ±604800 s. Every time difference in this package — and in a consumer differencing transmit times — must pass through this.\n\n\n\n\n\n","title":"PositionVelocityTime.fold_week_crossover"},{"category":"type","location":"api/#PositionVelocityTime.BiasColumns","page":"API Reference","text":"BiasColumns\n\nPer-satellite assignment of the estimated bias columns of the least-squares design matrix, shared by calc_ρ_hat!, calc_H! and user_position. The state vector is [x, y, z, tc₁, …, tc_num_clock_biases, ifb₁, …, ifb_num_ifb] with num_lsq_params(bias_columns) entries. The two column kinds have different physical sources: a clock column is the receiver clock for one GNSS time system (the spacing between systems is a system/space-segment effect — the broadcast GNSS time offset, GGTO or BGTO), whereas an inter-frequency-bias column is the receiver's per-band RF-chain delay. Known per-satellite corrections (atmosphere, the broadcast time-system offset) are applied to the pseudoranges in calc_pvt, not carried here.\n\nFields\n\nclock_bias_indices::Vector{Int}: per satellite, the clock column (1…num_clock_biases) of its GNSS time system; the design-matrix 1.0 lands at 3 + clock_bias_indices[j].\nnum_clock_biases::Int: number of clock biases (also the offset of the IFB block).\nifb_indices::Vector{Int}: per satellite, the inter-frequency-bias column (1…num_ifb) of its frequency band, or 0 for the reference band; the 1.0 lands at 3 + num_clock_biases + ifb_indices[j].\nnum_ifb::Int: number of inter-frequency biases (frequency bands beyond the reference).\n\n\n\n\n\n","title":"PositionVelocityTime.BiasColumns"},{"category":"function","location":"api/#PositionVelocityTime.decide_bias_layout","page":"API Reference","text":"decide_bias_layout(states, systems, bands)\n    -> Union{NamedTuple,Nothing}\n\nDecide the full least-squares bias layout — one clock column per GNSS time system plus the per-band inter-frequency-bias columns from band_ifb_layout — and return it as (; bias_columns, extra_bands, reference_bands, hub_system, hub_offset_decoders), or nothing when the constellation cannot be solved:\n\nbias_columns::BiasColumns: the per-satellite column assignment (see BiasColumns).\nextra_bands::Vector{Symbol}: band of each inter-frequency-bias column, so extra_bands[i] belongs to column i of the IFB block.\nreference_bands::Vector{Symbol}: per IFB column, the reference band of its coverage component — the anchor that column's bias is measured against (see band_ifb_layout).\nhub_system::Union{Nothing,GNSSSignals.TimeSystem}: the system the collapsed clocks were merged onto, or nothing for a layout that estimates every clock bias independently, which is the common case.\nhub_offset_decoders::Dict: per collapsed time system, the decoder whose broadcast offset to hub_system converts that system's measurements — the same vocabulary as time_offset_available, which decides membership, and calc_steering_offset, which evaluates one entry (see calc_hub_range_offsets). Empty exactly when hub_system is nothing.\n\nEvery input is a classification of the epoch, never a transmit time: the counts, the coverage graph and time_offset_available are all the decision needs. The offsets its hub_offset_decoders enable are built afterwards, once calc_pvt has the transmit times.\n\nThe decision is observability-driven, not merely count-driven:\n\nWhen the coverage graph is connected and the satellites suffice for the unknowns (n ≥ 3 + num_systems + num_ifb measurements, of which 3 + num_systems must come from distinct satellites — extra bands of an already-tracked satellite add inter-frequency-bias information, not geometry), estimate everything independently: the inter-system offset and the receiver inter-frequency biases are observed directly from the geometry, so neither inherits the broadcast-GGTO error (the satellite group delays are already removed per satellite upstream, so the per-band column carries the receiver chain).\nOtherwise merge every clock that can be merged onto a hub system, using a broadcast offset to that hub (see time_offset_available). Hubs are tried in the fixed order GPST, GST, BDT, and the first whose merged layout has enough satellites wins, so the choice is deterministic and a GPS-anchored collapse behaves exactly as it always did. Toward GPS Time the offsets on the air are Galileo's GGTO and BeiDou's BGTO; toward Galileo System Time they are BeiDou's BGTO variant and the GGTO GPS itself broadcasts on CNAV/CNAV-2 — which is what lets a GPS-free Galileo + BeiDou epoch, or a GPS + Galileo epoch whose Galileo satellites have not decoded their GGTO yet, still collapse. The merge removes a clock unknown per merged system (the scarce-satellite case) and reconnects a disjoint band split (the disconnected case — where a band's IFB column would otherwise be collinear with the stranded constellation's clock), making the inter-frequency bias observable again (it then carries the broadcast-offset error, alongside the offset-based inter-system bias). A system whose satellites carry no offset to the hub keeps its own clock column, so a mixed epoch can collapse Galileo and leave BeiDou independent, or the reverse.\nFailing that, fall back to the (already observability-restricted) independent layout if the satellite count allows. No IFB column is created for a band stranded on its own constellation, so its inter-frequency bias folds into that constellation's clock and the reported inter-system bias reads out as GGTO + (IFB difference) — the two are no longer separable. Position, residuals and DOP are still finite and non-degenerate; only the bias decomposition is ambiguous. Else return nothing.\n\nBecause band_ifb_layout never creates an unobservable IFB column, every returned layout is structurally sound — the degenerate disjoint-band case is removed by construction, not caught after the fact. The satellite conditions above are structural too, and thus necessary but not sufficient: a returned layout can still have degenerate geometry (lines of sight that span too little), which no satellite count can see. That is left to the checks calc_pvt makes on the solved geometry — the DOP's positive-definite test and the velocity solve's own — rather than pre-screened.\n\n\n\n\n\n","title":"PositionVelocityTime.decide_bias_layout"},{"category":"function","location":"api/#PositionVelocityTime.calc_ρ_hat!","page":"API Reference","text":"Computes ̂ρ, the distance between the satellite and the estimated user position\n\ncalc_ρ_hat!(ρ, sat_positions, ξ, bias_columns)\n\n\nξ: Estimated user position, one clock correction per GNSS time system, and one      receiver inter-frequency bias per frequency band beyond the reference band, i.e.      [x, y, z, tc₁, …, tc_num_clock_biases, ifb₁, …, ifb_num_ifb] sat_positions: Satellite Positions bias_columns: The per-satellite BiasColumns (clock column and      inter-frequency-bias column of each satellite). Known range corrections (atmosphere,      GGTO offset) are already folded into the measured ρ by calc_pvt.\n\n\n\n\n\n","title":"PositionVelocityTime.calc_ρ_hat!"},{"category":"function","location":"api/#PositionVelocityTime.calc_H","page":"API Reference","text":"calc_H(sat_positions, ξ, bias_columns::BiasColumns) -> Matrix{Float64}\n\nAllocating form of calc_H!: the least-squares design matrix at the state ξ, one row per satellite, with num_lsq_params(bias_columns) columns.\n\n\n\n\n\n","title":"PositionVelocityTime.calc_H"},{"category":"function","location":"api/#PositionVelocityTime.calc_line_of_sight","page":"API Reference","text":"Computes the unit line-of-sight vector from the user to the (Earth-rotation-corrected) satellite position — the textbook receiver→satellite direction. The pseudorange's position partial is its negative, which is what the design-matrix rows of calc_H! carry.\n\ncalc_line_of_sight(sat_pos, ξ)\n\n\nξ: Combination of estimated user position and time correction sat_pos: Single satellite positions\n\n\n\n\n\n","title":"PositionVelocityTime.calc_line_of_sight"},{"category":"function","location":"api/#PositionVelocityTime.calc_DOP","page":"API Reference","text":"Calculates the dilution of precision for a given geometry matrix H\n\nH_GEO has 3 + num_clock_biases + num_ifb columns (three position partials, one per GNSS time system, and one per frequency band beyond the reference). D = (HᵀH)⁻¹ is the parameter covariance in the units/frame of H. Because calc_H! builds H from ECEF unit vectors (the negated receiver→satellite lines of sight), the position block of D is in ECEF; it is rotated into the local ENU frame at user_pos before the horizontal/vertical split, so HDOP/VDOP are taken in the user's tangent plane (PDOP/GDOP are trace-invariant and unaffected by the rotation). GDOP spans all parameters; TDOP reports the clock variance of the primary (reference) system — see PVTSolution — while the other systems' clock (inter-system-bias) and the inter-frequency-bias variances enter GDOP only.\n\nA rank-deficient geometry makes HᵀH singular (not positive definite); positive_definite_cholesky detects this and the function returns the sentinel DOP(-1, …) instead of erroring.\n\ncalc_DOP(H_GEO, user_pos)\ncalc_DOP(H_GEO, user_pos, primary_clock_index)\n\n\nH_GEO: Geometry matrix user_pos: User ECEF position, for the ECEF→ENU rotation of the horizontal/vertical DOPs primary_clock_index: Index (1…numclockbiases) of the clock column whose variance is reported as TDOP\n\n\n\n\n\n","title":"PositionVelocityTime.calc_DOP"},{"category":"function","location":"api/#PositionVelocityTime.time_scale_offset_to_gpst","page":"API Reference","text":"time_scale_offset_to_gpst(time_system) -> Float64\n\nSigned offset of a GNSS time system's count against GPS Time's for the same instant — get_tai_offset(GPST) − get_tai_offset(time_system), so negative where the system's count reads lower (BDT: 19 − 33 = −14.0), and 0.0 for a system that counts alike. calc_time_scale_offsets adds the difference of two of these to a transmit time, which is what puts the +14 s onto a BeiDou time in a GPS-primary solve.\n\nThis is structural, not a bias: it follows from the time scales' definitions, not from either system's steering. Both GPST and GST are TAI − 19 s, so they count identically and this is 0.0 for every GPS and Galileo satellite. BDT is TAI − 33 s, so a BeiDou second-of-week reads 14 s lower than the GPS time-of-week for the same instant — BDT week 0 second 0 is GPS week 1356 time of week 14, the 14 leap seconds that had accrued between the two epochs.\n\nDerived from GNSSSignals.get_tai_offset rather than tabulated, so a constellation added later is covered without touching this.\n\nnote: Why this is a measurement correction and not a time-scale shift\nIt would be tempting to fold the 14 s into get_time_of_week and be done. That would be wrong: the satellite position is propagated from the same transmit time against the message's own t_0e, which is on the broadcasting system's scale, so shifting the reported time would move the 14 s into the ephemeris — about 55 km of along-track error at BeiDou MEO velocities. The transmit time must stay on its own scale and the correction must land on the pseudorange.\n\n\n\n\n\n","title":"PositionVelocityTime.time_scale_offset_to_gpst"},{"category":"function","location":"api/#PositionVelocityTime.calc_time_scale_offsets","page":"API Reference","text":"calc_time_scale_offsets(systems, primary_system) -> Vector{Float64}\n\nSeconds to add to each satellite's transmit time to express it in primary_system's count, so that calc_pseudo_ranges may difference them. 0.0 for every satellite of a system that counts alike — which is every GPS and Galileo satellite, and all of them in a single-constellation epoch.\n\nRelative to the primary system rather than to GPS Time, because the primary system's count is also what dates the reported epoch: reference_time comes out of the same differencing, and is combined with the primary system's week and system_start_epoch. Anchoring on GPST instead leaves a BeiDou-primary solve reporting a GPS-count time of week dated from the BDT epoch — a 14 s error in PVTSolution.time traded for the one this removes from the pseudoranges.\n\nThis is applied to the times handed to the differencing, and nowhere else. The satellite position must still be propagated from the untouched transmit time against the message's own t_0e, which is on the broadcasting system's scale, so shifting the time itself would move the offset into the ephemeris — about 55 km of along-track error at BeiDou MEO velocities. SatInfo.time and calc_steering_offset likewise keep the unconverted value.\n\n\n\n\n\n","title":"PositionVelocityTime.calc_time_scale_offsets"},{"category":"function","location":"api/#PositionVelocityTime.time_offset_available","page":"API Reference","text":"time_offset_available(decoder, target::GNSSSignals.TimeSystem) -> Bool\n\nWhether decoder carries a usable broadcast offset from its own GNSS time system to target. Such an offset lets that constellation's measurements be expressed on the target system's clock, which makes a fix possible when the geometry is too weak to estimate an independent clock bias for it — see decide_bias_layout, which collapses onto a hub system this way. Toward GPS Time that is Galileo's GGTO and BeiDou's BGTO; toward Galileo System Time it is BeiDou's BGTO variant and the GGTO GPS itself broadcasts on CNAV/CNAV-2.\n\nDelegates to GNSSDecoder's get_time_offset, which screens every way a signal can fail to have one: it broadcasts none toward target (GPS L1 C/A and Galileo E6-B broadcast none at all), it does but has not decoded one yet, the decoded one names a different target system, the ICD's \"not available\" sentinel is set (a GNSS_ID of 0; Galileo's all-ones GGTO), or — on Galileo — the reference week has not arrived yet, since word type 10 can be decoded before the week number is. Always false when target is the decoder's own system: the offset from a scale to itself is not a broadcast quantity.\n\n\n\n\n\n","title":"PositionVelocityTime.time_offset_available"},{"category":"function","location":"api/#PositionVelocityTime.calc_steering_offset","page":"API Reference","text":"calc_steering_offset(decoder, target::GNSSSignals.TimeSystem, t) -> Float64\n\nThe broadcast steering offset between this constellation's time scale and target's, in seconds, at its own time of week t. Subtract it to convert a transmit time to the target system's time. Only defined where time_offset_available is true for the same target.\n\nTens of nanoseconds: it is the residual between two atomic scales, not the whole difference between their counts. GNSSDecoder's GNSSTimeOffset.A_0 folds in the defined whole-second offset as well — so that t_target = t_own − Δt holds for the seconds — and this takes that part back out (as the difference of two time_scale_offset_to_gpst anchors, which is target-independent), because calc_time_scale_offsets has already applied it to the transmit times before they were differenced. Both use the same get_tai_offset expression, so the two halves compose exactly rather than approximately.\n\nnote: The subtraction costs about seven digits of the residual\nRecovering a ~1e-8 s residual by subtracting 14 s from a Float64 leaves roughly 1.8e-15 s of rounding — one ULP at 14. That is 0.5 µm of range, so it is irrelevant to a fix, but it is why the tests here compare the steering term with an explicit tolerance rather than the default ≈.\n\n\n\n\n\n","title":"PositionVelocityTime.calc_steering_offset"},{"category":"function","location":"api/#PositionVelocityTime.get_week","page":"API Reference","text":"get_week(decoder::GNSSDecoderState{<:GPSL1CAData}; approximate_year)\n\nReturn the absolute GPS week number for a GPSL1 decoder, resolving the 1024-week rollover ambiguity using approximate_year as a calendar anchor.\n\nThe legacy GPS L1 C/A LNAV message broadcasts only a 10-bit week number (0–1023) modulo 1024, so the receiver cannot determine which 1024-week cycle the recording is in from the data alone (IS-GPS-200, §20.3.3.3). Each cycle is ~19.6 years, so any anchor within ±9 years of the true observation date selects the correct cycle.\n\nGPS week 0 is 1980-01-06; cycle boundaries fall on 1999-08-22, 2019-04-07, 2038-11-21, 2058-07-08, …\n\nFor Galileo, the broadcast WN is 12 bits and does not need this treatment in any practical operational scenario.\n\n\n\n\n\n","title":"PositionVelocityTime.get_week"},{"category":"function","location":"api/#PositionVelocityTime.system_start_epoch","page":"API Reference","text":"system_start_epoch(system) -> TAIEpoch\n\nAbsolute TAI epoch of a ranging signal's GNSS time-scale origin (week 0, time of week 0), from GNSSSignals' get_tai_system_start_time — the epoch already labelled on the atomic scale (GPS 1980-01-06T00:00:19 TAI, Galileo 1999-08-22T00:00:19 TAI, BeiDou 2006-01-01T00:00:33 TAI), so no leap-second table is consulted. This used to go through get_system_start_time (a UTC label) and AstroTime's leap-aware conversion; GNSSSignals 4.1 states the TAI labels itself, which is also what makes the value safe to derive at precompile time.\n\n\n\n\n\n","title":"PositionVelocityTime.system_start_epoch"},{"category":"function","location":"api/#PositionVelocityTime.day_of_year","page":"API Reference","text":"day_of_year(system, week, time_of_week) -> Int\n\nDay of year (1–366) of the GNSS system time given by the absolute week and time_of_week (s) of system — the seasonal argument of the Niell tropospheric mapping (see tropospheric_delay). The system time scale's offset from UTC (leap seconds, ≤ ~18 s) is negligible for an argument with a one-year period.\n\n\n\n\n\n","title":"PositionVelocityTime.day_of_year"},{"category":"function","location":"api/#PositionVelocityTime.predict_atmospheric_delays","page":"API Reference","text":"predict_atmospheric_delays(ξ, states, sat_positions, correction,\n                           reference_time, doy, enable_tropospheric_correction) -> Vector{Float64}\n\nPer-satellite ionospheric + tropospheric delay (metres), to be subtracted from the pseudoranges. The user position is the first three elements of the least-squares state vector ξ = [x, y, z, tc₁, …] (ECEF, metres); the remaining clock-bias components are ignored. correction is the constellation-wide ionospheric model from select_ionospheric_correction (nothing skips the ionosphere); the troposphere uses the blind Saastamoinen zenith delays mapped by the Niell mapping functions, whose seasonal term takes the day of year doy (see tropospheric_delay), unless enable_tropospheric_correction is false.\n\nA single corrected solve is enough: the delays depend on position only through the satellite elevation/azimuth (and, for the troposphere, the user height), and ∂delay/∂position is negligible over the metre-level position uncertainty (a 15 m shift moves the elevation by ~1e-5°), so delays predicted at a nearby position are accurate to well under a millimetre — no iterate-to-convergence needed. The user geodetic coordinates and the ENU transform depend only on ξ, so they are computed once and reused across satellites.\n\n\n\n\n\n","title":"PositionVelocityTime.predict_atmospheric_delays"},{"category":"function","location":"api/#PositionVelocityTime.calc_course_over_ground","page":"API Reference","text":"calc_course_over_ground(position::ECEF, velocity::ECEF) -> typeof(1.0°)\n\nCourse over ground (degrees): the azimuth of velocity in the local East-North-Up frame at position, measured clockwise from true North and wrapped to [0, 360)°, following the GNSS COG convention. This is the direction of motion derived from the velocity, not vehicle heading. The vertical (Up) component is ignored. Returns 0° when the horizontal velocity is zero (stationary or purely vertical), where course is undefined.\n\nENUfromECEF is affine with position as its origin (which it maps to zero), so applying it to position + velocity rotates the velocity vector into the ENU frame without introducing any translation.\n\n\n\n\n\n","title":"PositionVelocityTime.calc_course_over_ground"},{"category":"function","location":"api/#PositionVelocityTime.calc_H!","page":"API Reference","text":"Computes Geometry Matrix H\n\nThe matrix has num_lsq_params(layout) columns: three for the position partials, one per GNSS time system, and one per frequency band beyond the reference band. Each row carries a 1.0 in its system's clock column (3 + clock_bias_indices[j]) and, unless the satellite is on the reference band, a 1.0 in its band's inter-frequency-bias column (3 + num_clock_biases + ifb_indices[j]); the remaining bias columns are zero.\n\ncalc_H!(H, sat_positions, ξ, bias_columns)\n\n\nξ: Estimated user position, per-system clock corrections, and per-band inter-frequency biases sat_positions: Matrix of satellite positions bias_columns: The per-satellite BiasColumns\n\n\n\n\n\n","title":"PositionVelocityTime.calc_H!"},{"category":"function","location":"api/#PositionVelocityTime.user_position","page":"API Reference","text":"Computes user position\n\nuser_position(sat_positions_mat, ρ, bias_columns)\nuser_position(sat_positions_mat, ρ, bias_columns, prev_ξ)\n\n\nsat_positions_mat: Satellite positions as a (3, N) matrix (xyz per satellite). ρ: Array of pseudo ranges\n\nCalculates the user position by least squares method. The algorithm is based on the common reception method.\n\nbias_columns: The per-satellite BiasColumns (clock and inter-frequency-bias columns).\n\nReturns (ξ, residuals): the solved state vector ξ = [x, y, z, tc₁, …, ifb₁, …] and the per-satellite post-fit residual vector (measured minus modeled pseudorange, metres), in the same satellite order as ρ.\n\nLsqFit reports its own residual as model - data, so the returned vector negates it. Measured − modeled (\"observed minus computed\") is how GNSS software reports observation residuals — RTKLIB's rescode, and GNSS-SDR and PocketSDR through it — and the negation is the whole of the difference: it is applied to the converged fit, so the solve itself is untouched.\n\n\n\n\n\n","title":"PositionVelocityTime.user_position"},{"category":"function","location":"api/#PositionVelocityTime.num_lsq_params","page":"API Reference","text":"num_lsq_params(bias_columns::BiasColumns) -> Int\n\nLength of the least-squares state vector for bias_columns: 3 + num_clock_biases + num_ifb.\n\n\n\n\n\n","title":"PositionVelocityTime.num_lsq_params"},{"category":"function","location":"api/#PositionVelocityTime.calc_hub_range_offsets","page":"API Reference","text":"calc_hub_range_offsets(hub_offset_decoders, hub_system, systems, times) -> Vector{Float64}\n\nPer-satellite range offsets (metres) that carry a clock collapse into the measurements, as decided by decide_bias_layout: all-zero for the satellites of a system that keeps its own clock unknown — all of them when hub_offset_decoders is empty — and otherwise −c · Δt_systems for each satellite of a collapsed system, evaluated at its own transmit time.\n\nThe broadcast offset is Δt_systems = (that system's time) − (the hub system's) (see calc_steering_offset: the GGTO for Galileo toward a GPS hub, the BGTO for BeiDou toward either), so a transmit time becomes hub time by SUBTRACTING it; the modeled range therefore carries −c·Δt_systems, and the solve yields inter_system_biases[sys] = −c·Δt_systems. Which satellite of a system reported the offset does not matter — it is one constellation-wide value — so decide_bias_layout picks the first decoded copy per system and it converts all of that system's measurements.\n\n\n\n\n\n","title":"PositionVelocityTime.calc_hub_range_offsets"},{"category":"function","location":"api/#PositionVelocityTime.positive_definite_cholesky","page":"API Reference","text":"positive_definite_cholesky(A::Symmetric) -> Union{Cholesky,Nothing}\n\nCholesky factorization of A, or nothing when A is not positive definite to within the rank tolerance below. Used to solve and invert the normal-equations matrices of this package (HᵀH for a design matrix H), which are positive definite exactly when H has full column rank — so nothing means the geometry left some parameter unobservable, and the epoch cannot be solved.\n\nCholesky is the right factorization for a symmetric positive definite matrix (≈2× cheaper than a general or symmetric-indefinite one, and numerically faithful), and check = false reports the non-positive-definite case instead of throwing. issuccess alone is not a sufficient test, for two reasons:\n\nStaticArrays accepts a pivot of exactly zero (its check is pivot ≥ 0) and returns a \"successful\" factor whose diagonal contains that zero — the triangular solve that follows then throws the very SingularException this is meant to avoid.\nA rank-deficient design usually does not produce an exactly zero pivot at all: rounding leaves a tiny positive one instead, and the factorization \"succeeds\" with a solution made of rounding noise (velocities of 1e8 m/s and the like).\n\nBoth are caught by a relative rank tolerance on the Cholesky diagonal, which for a normal-equations matrix is on the scale of H's singular values — so its smallest-to-largest ratio is ~1/cond(H). A rank-deficient design leaves the ratio at rounding level (~1e-8 for Float64, or exactly 0), whereas even a barely usable GNSS geometry stays above ~1e-3; cbrt(eps) ≈ 6e-6 sits between the two with orders of magnitude of margin either way. The test costs a few comparisons and does not allocate.\n\npositive_definite_cholesky(A)\n\n\n\n\n\n\n","title":"PositionVelocityTime.positive_definite_cholesky"},{"category":"function","location":"api/#PositionVelocityTime.band_ifb_layout","page":"API Reference","text":"band_ifb_layout(systems, bands) -> (ifb_indices, extra_bands, reference_bands, num_components)\n\nLay out the receiver inter-frequency biases from the (constellation × band) coverage graph. Two bands share a coverage component iff some constellation is tracked on both (directly or through a chain of shared constellations); within a component the clock/IFB split has a single gauge freedom, so one reference band per component (the most-populated, ties by first appearance) is fixed and an IFB column is created only for the other bands of that component. This estimates exactly the observable IFBs — a band that is the sole band of its component (its constellation lives only there) gets none, its delay folding into that constellation's clock — so the resulting design matrix is always full rank.\n\nifb_indices[j] is satellite j's IFB column (1…length(extra_bands)), or 0 for a per-component reference band; extra_bands[i] is the band of IFB column i (ordered deterministically) and reference_bands[i] is the reference band of that column's coverage component (the anchor its IFB is measured against); num_components is the number of coverage components (1 ⇔ the graph is connected).\n\n\n\n\n\n","title":"PositionVelocityTime.band_ifb_layout"},{"category":"section","location":"#PositionVelocityTime.jl","page":"Home","text":"Calculates position, velocity, and time from GNSS satellite measurements.","title":"PositionVelocityTime.jl"},{"category":"section","location":"#Features","page":"Home","text":"Estimation of user position, velocity, and time\nSatellite position and velocity calculation from orbital parameters: the directly-broadcast Keplerian ephemerides (GPS LNAV, Galileo I/NAV and F/NAV, BeiDou D1/D2), the quasi-Keplerian ones (GPS CNAV/CNAV-2, BeiDou B-CNAV1/2/3), and the BeiDou GEO reference frame\nDilution of Precision (DOP) computation\nSupport for GPS (L1 C/A, L2C, L5, L1C), Galileo (E1B, E5a, E5b) and BeiDou (B1I, B3I, B1C, B2a, B2b), including combined multi-GNSS solutions. Each measurement is one satellite-band pseudorange; the group-delay/ISC correction is selected by the signal the range was generated on (which may be a pilot such as GPS L1C-P, Galileo E1C or BeiDou B2a-Q), while the ephemeris and clock come from the band's data-component decoder.\nOne receiver clock bias per GNSS time system (GPST, GST, BDT), collapsed onto GPS Time using the broadcast offset — Galileo's GGTO or BeiDou's BGTO — when the geometry is too weak to estimate it\n\nGalileo E6-B is decoded by GNSSDecoder but carries no ephemeris of its own: its C/NAV message is the Galileo High Accuracy Service correction channel. An E6-B satellite state can be passed to calc_pvt safely — it is recognised and excluded from the solve — but it contributes no pseudorange, and the HAS corrections it carries are not yet applied here.","title":"Features"},{"category":"section","location":"#Installation","page":"Home","text":"using Pkg\nPkg.add(\"PositionVelocityTime\")","title":"Installation"},{"category":"section","location":"#Quick-Start","page":"Home","text":"Decoded data and code phase of a satellite must be combined in the SatelliteState struct:\n\nusing PositionVelocityTime, GNSSSignals, GNSSDecoder\n\ngpsl1 = GPSL1CA()\nsat_state = SatelliteState(\n    decoder = decoder,\n    system = gpsl1,\n    code_phase = code_phase,\n    carrier_doppler = carrier_doppler,\n    carrier_phase = carrier_phase,  # optional, in radians\n)\n\ncode_phase is in chips and carrier_phase in radians, matching Tracking's get_code_phase and get_carrier_phase.\n\nAlternatively, pass a Tracking.TrackedSat directly — tracked_sat is what Tracking.get_sat_state returns for a tracked satellite, and the code phase, carrier Doppler, and carrier phase are read off it:\n\nusing Tracking\nsat_state = SatelliteState(decoder, gpsl1, tracked_sat)\n\nCompute the PVT solution:\n\npvt = calc_pvt(sat_states)\nlla = get_LLA(pvt)  # latitude, longitude, altitude\n\nSatellites from different constellations may be passed together. Because each GNSS references its broadcasts to its own system time, calc_pvt estimates one receiver clock bias per GNSS time system, so a combined fix needs at least 3 + M satellites for M distinct systems. The per-system clock offsets are reported as pvt.inter_system_biases relative to pvt.reference_system.\n\nIf too few healthy satellites are tracked to solve the constellation — or the geometry turns out to be degenerate — calc_pvt returns the prev_pvt it was given (the origin solution by default) rather than throwing, so a receiver can hand it whatever it currently tracks each epoch and carry the last solution forward.","title":"Quick Start"}]
}
